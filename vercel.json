{
  "buildCommand": "cd demo && npm install && node_modules/.bin/vite build && cp -r dist ../demo-out",
  "outputDirectory": "demo-out",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
